[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --strict-markers
markers =
    slow: testes lentos (rede, modelos LLM)
    integration: testes que precisam do server.py rodando
